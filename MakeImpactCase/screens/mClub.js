import React from "react";
import { View, Text, StyleSheet } from "react-native"

const mClub = props => {

    return (
        <View>
            <Text>This is the Club Page</Text>
        </View>
     );
    }

    const styles = StyleSheet.create({
 
    });

export default function mClub();